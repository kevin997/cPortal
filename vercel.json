{
  "framework": "nextjs",
  "buildCommand": "prisma migrate deploy && prisma generate && next build --webpack",
  "installCommand": "npm install",
  "env": {
    "VERCEL_FORCE_NO_BUILD_CACHE": "1"
  }
}
